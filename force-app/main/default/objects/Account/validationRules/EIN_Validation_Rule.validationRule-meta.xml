<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EIN_Validation_Rule</fullName>
    <active>true</active>
    <description>EIN must be in the format XX-XXXXXXX (e.g., 12-3456789).</description>
    <errorConditionFormula>AND(
    NOT(ISBLANK(EIN__c)),
    NOT(REGEX(EIN__c, &quot;^[0-9]{2}-[0-9]{7}$&quot;))
)</errorConditionFormula>
    <errorDisplayField>EIN__c</errorDisplayField>
    <errorMessage>EIN must be in the format XX-XXXXXXX 
(e.g., 12-3456789).</errorMessage>
</ValidationRule>
